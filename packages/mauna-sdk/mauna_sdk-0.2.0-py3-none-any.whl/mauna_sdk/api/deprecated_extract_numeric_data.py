#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field as _field
from ..schema_config.json_scalar import custom_scalars
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin, config


# fmt: off
QUERY: List[str] = ["""
query deprecatedExtractNumericData($text: String!) {
  callNlpDoc(text: $text) {
    tokens {
      numeric_analysis: extension {
        data: nfh_head
        has_numeric: is_nfh
      }
    }
  }
}

"""
]


class deprecatedExtractNumericData:
    @dataclass(frozen=True)
    class deprecatedExtractNumericDataData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class NlpDoc(DataClassJsonMixin):
            @dataclass(frozen=True)
            class Token(DataClassJsonMixin):
                @dataclass(frozen=True)
                class TokenExtension(DataClassJsonMixin):
                    data: Optional[str]
                    has_numeric: Optional[bool]

                numeric_analysis: Optional[TokenExtension]

            tokens: Optional[List[Token]]

        callNlpDoc: Optional[NlpDoc]

    # fmt: off
    @classmethod
    def execute(cls, client: Client, text: str) -> Optional[deprecatedExtractNumericDataData.NlpDoc]:
        variables: Dict[str, Any] = {"text": text}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.deprecatedExtractNumericDataData.from_dict(response_text)
        return res.callNlpDoc

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client, text: str) -> Optional[deprecatedExtractNumericDataData.NlpDoc]:
        variables: Dict[str, Any] = {"text": text}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.deprecatedExtractNumericDataData.from_dict(response_text)
        return res.callNlpDoc
