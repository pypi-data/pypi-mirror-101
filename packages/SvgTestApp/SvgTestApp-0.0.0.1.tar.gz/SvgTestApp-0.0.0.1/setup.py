from setuptools import setup

setup(
    name='SvgTestApp',
    version='0.0.0.1',
    description="This is just a test",
    packages=['script'],
    install_requires=['svgwrite'],
    python_requires='>=3',
)
