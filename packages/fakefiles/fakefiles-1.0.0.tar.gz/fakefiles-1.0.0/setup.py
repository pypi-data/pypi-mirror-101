
from setuptools import setup

setup(
    name='fakefiles',
    version='1.0.0',
    description='A package to generate files that are not really there, but can be used like normal.',
    long_description='''Will add later''',
    packages=["fakefiles"]
    )

