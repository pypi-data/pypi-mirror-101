/**
 * configuration for module.
 */

requirejs.config({
    baseUrl : '.', 
    paths : {
        Triangle : 'triangle', 
        Line : 'line', 
        Graph : 'graph', 
        Model : 'model'
    }
}) ; 