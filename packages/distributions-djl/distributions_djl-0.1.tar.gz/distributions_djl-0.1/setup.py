from setuptools import setup

setup(name='distributions_djl',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_djl'],
      author='Derrick Lewis',
      zip_safe=False)
